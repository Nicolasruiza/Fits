const DATA=window.FITS_DATA;const defaults=Object.fromEntries(DATA.pieces.map(p=>[p.id,p.status]));let wardrobe=JSON.parse(localStorage.getItem('fitsWardrobe')||'null')||defaults;const piece=id=>DATA.pieces.find(p=>p.id===id),look=id=>DATA.looks.find(l=>l.id===id),family=id=>DATA.families.find(f=>f.id===id),missingFor=l=>l.pieces.filter(id=>wardrobe[id]!=='owned'),familyLooks=f=>f.looks.map(look).filter(Boolean),readyVariants=f=>familyLooks(f).filter(l=>missingFor(l).length===0);function save(){localStorage.setItem('fitsWardrobe',JSON.stringify(wardrobe));renderCurrent()}function nav(a){return `<nav class="nav"><a href="index.html" class="${a==='looks'?'active':''}"><b>▦</b>Looks</a><a href="wardrobe.html" class="${a==='wardrobe'?'active':''}"><b>◇</b>Wardrobe</a><a href="unlock.html" class="${a==='unlock'?'active':''}"><b>↗</b>Unlock</a></nav>`}function top(t,s,r=''){return `<header class="topbar"><div class="brandrow"><div class="brand">${t}</div><div class="kicker">${r}</div></div><div class="subtitle">${s}</div></header>`}function meter(n){return `<div class="meter">${[0,1,2,3,4].map(i=>`<span class="dot ${i<n?'on':''}"></span>`).join('')}</div>`}function familyCard(f){const vs=familyLooks(f),h=look(f.hero)||vs[0],ready=readyVariants(f).length,b=[...new Set(vs.flatMap(v=>v.badges))],formal=Math.round(vs.reduce((s,v)=>s+v.formal,0)/vs.length);return `<a class="look-card family-card" href="detail.html?family=${f.id}"><div class="look-image family-image">${vs.length>1?`<div class="variant-stack">${vs.slice(1,3).map((v,i)=>`<img class="variant-peek peek-${i+1}" src="${v.image}">`).join('')}</div>`:''}<img class="hero-img" src="${h.image}"><span class="pill ${ready?'ready':''}">${ready?`${ready}/${vs.length} ready`:'Incomplete'}</span>${vs.length>1?`<span class="variant-count">${vs.length} variants</span>`:''}</div><div class="look-body"><h3>${f.name}</h3>${meter(formal)}<div class="badges">${b.map(x=>`<span class="badge">${x}</span>`).join('')}</div></div></a>`}function renderLooks(){const ready=DATA.families.filter(f=>readyVariants(f).length).length;document.body.innerHTML=`<div class="app">${top('Fits','Curated outfit families',`${DATA.families.length} families`)}<main><div class="chips"><button class="chip active">All</button><button class="chip">Office</button><button class="chip">Weekend</button><button class="chip">Dinner</button><button class="chip">Travel</button></div><div class="stats" style="margin-top:13px"><div class="stat"><b>${DATA.families.length}</b><span>style families</span></div><div class="stat"><b>${DATA.looks.length}</b><span>real variants</span></div><div class="stat"><b>${ready}</b><span>wearable now</span></div></div><div class="notice">Home shows the idea, not every permutation. Similar looks stay together so the library remains compact.</div><div class="grid">${DATA.families.map(familyCard).join('')}</div></main>${nav('looks')}</div>`}function renderWardrobe(){const owned=DATA.pieces.filter(p=>wardrobe[p.id]==='owned').length,missing=DATA.pieces.filter(p=>wardrobe[p.id]==='missing').length,unsure=DATA.pieces.length-owned-missing;document.body.innerHTML=`<div class="app">${top('Wardrobe','Mark what you actually own',`${DATA.pieces.length} items`)}<main><div class="stats"><div class="stat"><b>${owned}</b><span>owned</span></div><div class="stat"><b>${missing}</b><span>missing</span></div><div class="stat"><b>${unsure}</b><span>unsure</span></div></div><div class="ward-list">${DATA.pieces.map(p=>`<div class="ward-item"><div class="ward-info"><strong>${p.name}</strong><small>${p.category}</small></div><div class="status-group">${['owned','missing','unsure'].map(s=>`<button class="status-btn ${wardrobe[p.id]===s?'active '+s:''}" onclick="wardrobe['${p.id}']='${s}';save()">${s==='owned'?'Tengo':s==='missing'?'No tengo':'Dudoso'}</button>`).join('')}</div></div>`).join('')}</div></main>${nav('wardrobe')}</div>`}function renderUnlock(){const items=DATA.pieces.filter(p=>wardrobe[p.id]!=='owned').map(p=>{let used=0,completes=0,fs=new Set;DATA.looks.forEach(l=>{if(l.pieces.includes(p.id)){const m=missingFor(l);if(m.includes(p.id)){used++;fs.add(l.family);if(m.length===1)completes++}}});return {...p,used,completes,familiesHelped:fs.size}}).filter(x=>x.used).sort((a,b)=>b.completes-a.completes||b.familiesHelped-a.familiesHelped||b.used-a.used);document.body.innerHTML=`<div class="app">${top('Unlock','Buy for coverage, not clutter','coverage')}<main><div class="notice">Ranking uses real variants but also counts how many style families each missing piece helps.</div>${items.map((p,i)=>`<div class="unlock-card"><div class="unlock-top"><strong>${i+1}. ${p.name}</strong><span class="unlock-score">${p.completes?`completes ${p.completes}`:`${p.familiesHelped} families`}</span></div><p>${p.completes?`Adding this would immediately complete ${p.completes} variant${p.completes>1?'s':''}. `:''}It appears in ${p.used} variant${p.used>1?'s':''} across ${p.familiesHelped} style famil${p.familiesHelped===1?'y':'ies'}.</p></div>`).join('')}</main>${nav('unlock')}</div>`}function variantDifference(f,v){const vs=familyLooks(f),base=vs[0];if(v.id===base.id)return'Base look';const add=v.pieces.filter(x=>!base.pieces.includes(x)).map(x=>piece(x)?.name).filter(Boolean),rem=base.pieces.filter(x=>!v.pieces.includes(x)).map(x=>piece(x)?.name).filter(Boolean);if(add.length===1&&rem.length===1)return`${rem[0]} → ${add[0]}`;if(add.length===1&&!rem.length)return`Add ${add[0]}`;if(!add.length&&rem.length===1)return`Without ${rem[0]}`;return v.name}function renderDetail(){const q=new URLSearchParams(location.search),f=family(q.get('family'))||DATA.families[0],vs=familyLooks(f),active=look(q.get('look'))||look(f.hero)||vs[0],miss=missingFor(active);document.body.innerHTML=`<div class="app"><header class="topbar"><a class="back" href="index.html">← Back to looks</a><div class="brandrow"><div class="brand">${f.name}</div><div class="kicker">${vs.length>1?`${vs.length} variants`:'single look'}</div></div></header><main><div class="detail-hero"><img src="${active.image}"></div><div class="detail-head"><h1>${active.name}</h1><p>${miss.length?`${miss.length} item${miss.length>1?'s':''} missing in this variant`:'Ready with your current wardrobe'}</p></div>${vs.length>1?`<section class="variants"><div class="variant-heading"><h2>Variants</h2><span>${vs.length} real combinations</span></div><div class="variant-strip">${vs.map((v,i)=>`<a class="variant-option ${v.id===active.id?'active':''}" href="detail.html?family=${f.id}&look=${v.id}"><img src="${v.image}"><div><strong>Option ${i+1}</strong><small>${variantDifference(f,v)}</small></div></a>`).join('')}</div></section>`:''}<section class="collection"><h2>Pieces in this variant</h2><div class="product-grid">${active.pieces.map(pid=>{const p=piece(pid),st=wardrobe[pid];return `<div class="product-card">${st==='missing'?'<span class="missing-tag">MISSING</span>':''}<div class="product-visual"><div class="shape"></div></div><strong>${p.name}</strong><small>${st==='owned'?'In wardrobe':st==='missing'?'Not in wardrobe':'Confirm item'}</small></div>`}).join('')}</div></section></main>${nav('looks')}</div>`}function renderCurrent(){const p=document.body.dataset.page;if(p==='looks')renderLooks();if(p==='wardrobe')renderWardrobe();if(p==='unlock')renderUnlock();if(p==='detail')renderDetail()}document.addEventListener('DOMContentLoaded',renderCurrent);