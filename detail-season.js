(()=>{const D=window.FITS_DATA;if(!D)return;const q=new URLSearchParams(location.search),fam=D.families.find(f=>f.id===q.get('family'))||D.families[0],active=D.looks.find(l=>l.id===q.get('look'))||D.looks.find(l=>l.id===fam.hero)||D.looks.find(l=>(fam.looks||[]).includes(l.id));function enhance(){const row=document.querySelector('.statusrow');if(!row||row.querySelector('.season-status'))return;(active?.seasons||[]).slice().reverse().forEach(s=>{const el=document.createElement('span');el.className='status season-status';el.textContent=s;row.prepend(el)})}new MutationObserver(()=>requestAnimationFrame(enhance)).observe(document.getElementById('root')||document.body,{childList:true,subtree:true});enhance();})();